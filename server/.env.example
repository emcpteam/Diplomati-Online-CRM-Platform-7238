# Esempio file di configurazione per il server email
PORT=3001

# SendGrid Configuration (Recommended)
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Default SMTP Settings (Optional)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your_sendgrid_api_key

# From Email Settings
FROM_EMAIL=noreply@diplomatonline.it
FROM_NAME=Diplomati Online